name: CI
on: [push]
jobs:
      build:
        runs-on: macOS-latest

        steps:
        - name: Checkout
          uses: actions/checkout@master
      #- name: Testing....
      # run: ls /Applications | grep Xcode
        - name: iPhone 11 Test
          run:  xcodebuild test -project GithubCIIntegration.xcodeproj -scheme GithubCIIntegration -sdk iphonesimulator -destination "platform=iOS Simulator,name=iPhone 6,OS=12.2"
